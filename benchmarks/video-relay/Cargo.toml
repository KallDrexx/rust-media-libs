[package]
name = "video-relay"
version = "0.1.0"
authors = ["Matthew Shapiro <me@mshapiro.net>"]
description = "Benchmark to test the speed of one publisher relaying video to two players"

[dependencies]
rml_rtmp = { path = "../../rtmp" }
rml_amf0 = "0.1"
bytes = "0.4"
